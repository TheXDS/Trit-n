<TabItem
    x:Class="TheXDS.Triton.Pages.TabHost"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
    xmlns:local="clr-namespace:TheXDS.Triton.Pages"
    mc:Ignorable="d"
    xmlns:vc ="clr-namespace:System.Windows.Converters;assembly=MCART.WPF"
    xmlns:vm="clr-namespace:TheXDS.Triton.ViewModels"
    xmlns:vm2="clr-namespace:TheXDS.Triton.ViewModels;assembly=TritonUi"
    d:DataContext="{d:DesignInstance vm:TabHostViewModel}">
    <TabItem.Resources>
        <vc:McartColorConverter x:Key="McartColorConverter"/>
    </TabItem.Resources>
    <TabItem.Header>
        <DockPanel>
            <Separator DockPanel.Dock="Bottom" Height="3" Margin="0,0,0,-2" Background="{Binding AccentColor, Converter={StaticResource McartColorConverter}}"/>
            <Button Width="16" BorderThickness="0" Background="Transparent" DockPanel.Dock="Right" Command="{Binding CloseCommand, Mode=OneWay}">x</Button>
            <TextBlock Margin="0,0,5,0" Text="{Binding Title}"/>
        </DockPanel>
    </TabItem.Header>
    <TabItem.Content>
        <Frame NavigationUIVisibility="Hidden" Source="/WpfClient;component/Pages/TestPage.xaml"/>
    </TabItem.Content>
</TabItem>
